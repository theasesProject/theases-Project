import { StyleSheet, Text, View } from "react-native";

const UserProfileSettings = () => {
  return (
    <View style={styles.editProfilePage}>
      <Text>UserProfileSettings</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  editProfilePage: {
    flex: 1,
  },
});

export default UserProfileSettings;
